# Format notes

Independent documentation of the binary formats Latent parses lives here, one file per format, written as each extractor lands: EVTX chunks and binary XML, journald files, utmp records, registry hives and their transaction logs, and so on.

Keeping these notes in the repository and separate from the code lets a reviewer check the parser against the format instead of against itself.
