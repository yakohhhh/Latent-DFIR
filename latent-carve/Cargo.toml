[package]
name = "latent-carve"
description = "Extractor interface and per-format record extractors"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
latent-core = { path = "../latent-core", version = "0.1.0" }
latent-source = { path = "../latent-source", version = "0.1.0" }
crc32fast = "1"
thiserror = "2.0"
tracing = "0.1"

[dev-dependencies]
tempfile = "3"
